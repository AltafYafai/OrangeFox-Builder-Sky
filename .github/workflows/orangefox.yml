name: OrangeFox Build [Sky]

on:
  workflow_dispatch:
    inputs:
      MANIFEST_BRANCH:
        description: 'OrangeFox Manifest Branch'
        required: true
        default: '12.1'
        type: choice
        options:
        - 12.1
        - 11.0
      DEVICE_TREE:
        description: 'Custom Recovery Tree'
        required: true
        default: 'https://github.com/AltafYafai/device_xiaomi_sky-recovery'
      DEVICE_TREE_BRANCH:
        description: 'Custom Recovery Tree Branch'
        required: true
        default: 'ofox-12.1'
      DEVICE_PATH:
        description: 'Specify your device path.'
        required: true
        default: 'device/xiaomi/sky'
      DEVICE_NAME:
        description: 'Specify your Device Codename.'
        required: true
        default: 'sky'
      BUILD_TARGET:
        description: 'Specify your Build Target'
        required: true
        default: 'recovery'
        type: choice
        options:
        - recovery
        - boot
        - vendorboot

jobs:
  build:
    name: Build OrangeFox for ${{ github.event.inputs.DEVICE_NAME }}
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
    permissions:
      contents: write
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Clean-up
      uses: rokibhasansagar/slimhub_actions@main

    - name: Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 12

    - name: Build Environment
      run: |
        sudo apt update
        sudo apt install aria2 curl -y
        git clone https://gitlab.com/OrangeFox/misc/scripts
        cd scripts
        sudo bash setup/android_build_env.sh

    - name: Set-up Manifest
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/OrangeFox
        cd ${GITHUB_WORKSPACE}/OrangeFox
        git config --global user.name "altafyafai"
        git config --global user.email "altaf.alu@gmail.com"
        git clone https://gitlab.com/OrangeFox/sync.git
        cd sync
        ./orangefox_sync.sh --branch ${{ github.event.inputs.MANIFEST_BRANCH }} --path ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}

    - name: Clone Device Tree
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        git clone ${{ github.event.inputs.DEVICE_TREE }} -b ${{ github.event.inputs.DEVICE_TREE_BRANCH }} ./${{ github.event.inputs.DEVICE_PATH }}
        cd ${{ github.event.inputs.DEVICE_PATH }}
        echo "COMMIT_ID=$(git rev-parse HEAD)" >> $GITHUB_ENV
        echo "COMMIT_MSG=$(git log -1 --pretty=%B | head -n 1)" >> $GITHUB_ENV

    - name: Building OrangeFox
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        set +e
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        export WITHOUT_CHECKBUILD=true
        set -e
        lunch fox_${{ github.event.inputs.DEVICE_NAME }}-eng
        make clean
        mka adbd ${{ github.event.inputs.BUILD_TARGET }}image

    - name: Set Build Date
      run: |
        echo "BUILD_DATE=$(TZ=Asia/Kolkata date +%Y%m%d)" >> $GITHUB_ENV

    - name: Prepare Artifacts
      id: prep
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/
        mkdir -p ${GITHUB_WORKSPACE}/artifacts/
        cp recovery.img ${GITHUB_WORKSPACE}/artifacts/ 2>/dev/null || true
        cp boot.img ${GITHUB_WORKSPACE}/artifacts/ 2>/dev/null || true
        cp vendor_boot.img ${GITHUB_WORKSPACE}/artifacts/ 2>/dev/null || true
        # Rename zip to a more consistent format if multiple are found
        find . -maxdepth 1 -name "OrangeFox*.zip" -exec cp {} ${GITHUB_WORKSPACE}/artifacts/OrangeFox-Unofficial-${{ github.event.inputs.DEVICE_NAME }}.zip \;
        find . -maxdepth 1 -name "OrangeFox*.img" -exec cp {} ${GITHUB_WORKSPACE}/artifacts/OrangeFox-Unofficial-${{ github.event.inputs.DEVICE_NAME }}.img \;
        
        if [ "$(ls -A ${GITHUB_WORKSPACE}/artifacts/)" ]; then
          echo "FILES_FOUND=true" >> $GITHUB_OUTPUT
          echo "ZIP_PATH=$(ls ${GITHUB_WORKSPACE}/artifacts/*.zip | head -n 1)" >> $GITHUB_ENV
        else
          echo "FILES_FOUND=false" >> $GITHUB_OUTPUT
        fi

    - name: Upload Release
      if: steps.prep.outputs.FILES_FOUND == 'true'
      uses: softprops/action-gh-release@v2
      with:
        files: ${{ github.workspace }}/artifacts/*
        name: OrangeFox Recovery for ${{ github.event.inputs.DEVICE_NAME }} [A14]
        tag_name: ${{ env.BUILD_DATE }}-${{ github.run_id }}
        body: |
          # 🦊 OrangeFox Recovery Project [A14]
          
          **Device:** Redmi 12 5G / Poco M6 Pro 5G / Redmi Note 12R (`${{ github.event.inputs.DEVICE_NAME }}`)
          **Build Date:** `${{ env.BUILD_DATE }}`
          **Manifest:** `OrangeFox-${{ github.event.inputs.MANIFEST_BRANCH }}`
          **Variant:** `A14 (Android 14)`
          **Maintainer:** [Altaf Yafai](https://github.com/AltafYafai)
          
          ---
          
          ### 🛠 Build Information
          - **Commit ID:** `${{ env.COMMIT_ID }}`
          - **Commit Message:** `${{ env.COMMIT_MSG }}`
          - **Device Tree:** [Here](${{ github.event.inputs.DEVICE_TREE }}/tree/${{ github.event.inputs.DEVICE_TREE_BRANCH }})
          
          ### 📥 Downloads
          - **GitHub Releases:** [Click Here](${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ env.BUILD_DATE }}-${{ github.run_id }})
          
          ---
          *Automated Build by OrangeFox Builder Sky*

    - name: Send Telegram Notification
      if: steps.prep.outputs.FILES_FOUND == 'true' && env.TG_BOT_TOKEN != '' && env.TG_CHAT_ID != ''
      run: |
        MSG="🔥 *OrangeFox Recovery for ${{ github.event.inputs.DEVICE_NAME }} [A14]*
        
        🔹 *Device:* \`Redmi 12 5G (sky)\`
        🔹 *Date:* \`${{ env.BUILD_DATE }}\`
        🔹 *Variant:* \`A14 (Android 14)\`
        🔹 *Maintainer:* [Altaf Yafai](https://github.com/AltafYafai)
        
        📦 *Build Info:*
        • [Device Tree](${{ github.event.inputs.DEVICE_TREE }})
        • Commit: \`${{ env.COMMIT_ID }}\`
        • Message: \`${{ env.COMMIT_MSG }}\`
        
        🔗 [Download from GitHub](${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ env.BUILD_DATE }}-${{ github.run_id }})
        
        #OrangeFox #Recovery #Sky #Android14"
        
        # Upload the ZIP file to Telegram
        curl -F chat_id="${TG_CHAT_ID}" -F document=@"${ZIP_PATH}" -F caption="${MSG}" -F parse_mode="markdown" https://api.telegram.org/bot${TG_BOT_TOKEN}/sendDocument
